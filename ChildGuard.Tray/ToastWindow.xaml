<Window x:Class="ChildGuard.Tray.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True" Width="340" Height="160" Background="#FF2D2D30"
        AllowsTransparency="True" Opacity="0.96">
    <Border x:Name="RootBorder" CornerRadius="8" Background="#FF2D2D30" BorderBrush="#FF3E3E42" BorderThickness="1" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="10" Height="10" Fill="#FFC29400" Margin="0,2,8,0" x:Name="IconDot"/>
                <TextBlock x:Name="TitleText" Text="ChildGuard" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
            <StackPanel Grid.Row="1" Margin="0,8,0,8">
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" Foreground="#FFDADADA"/>
                <TextBlock x:Name="DetailsText" TextWrapping="Wrap" Foreground="#FF9FA0A0" Margin="0,6,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,0,0,0">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Margin" Value="8,0,0,0"/>
                    </Style>
                </StackPanel.Resources>
                <Button x:Name="Snooze5Btn" Content="Snooze 5m" Padding="8,4"/>
                <Button x:Name="Snooze15Btn" Content="Snooze 15m" Padding="8,4"/>
                <Button x:Name="CloseBtn" Content="Close" Padding="8,4"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

